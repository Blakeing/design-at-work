import NavBar from "@/components/NavBar";

import { FC } from "react";

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return (
    <div>
      <NavBar />
    </div>
  );
};

export default Page;
